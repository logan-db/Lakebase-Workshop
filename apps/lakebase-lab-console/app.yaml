command:
  - "python"
  - "-m"
  - "uvicorn"
  - "app:app"
  - "--host"
  - "0.0.0.0"
  - "--port"
  - "8000"

env:
  - name: LAKEBASE_PROJECT_ID
    # >>> Set this to YOUR project ID from notebook 00 output <<<
    # Format: lakebase-lab-<your-username>  (e.g. lakebase-lab-jane-doe)
    value: "lakebase-lab-logan-rupert"
  - name: LAKEBASE_BRANCH_ID
    value: "production"
  - name: LAKEBASE_SCHEMA
    # >>> Set this to YOUR schema from notebook 00 output <<<
    # Format: lakebase_lab_<your_username>  (e.g. lakebase_lab_jane_doe)
    value: "lakebase_lab_logan_rupert"

resources:
  - name: lakebase-db
    type: postgres
